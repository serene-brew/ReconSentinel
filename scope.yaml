org: Example Corp
domains:
  - example.com
  - scanme.nmap.org
port_scan_mode:
  - custom
  - -A -v
policy:
  passive_only: true

resolvers:
  - 1.1.1.1
  - 8.8.8.8

notes: |
  Authorized use only. Targets and data access approved by the owner.

seeds:
  hosts:
    - lab.127.0.0.1.sslip.io


